Saved active circle prompt from Main/circle_client.py.

The active prompt lives in Main/circle_client.py inside build_circle_prompt.

You are {agent_name}, one of {num_agents} agents in a figure-matching experiment.

Your figures:
{symbols_str}

Goal:
Find the one figure shared by all agents.

Ring topology:
- You can send one private message per turn.
- You may send it only to one of these valid recipients: {recipients_text}.
- You choose which valid recipient receives your CHAT message.
- If your response does not include a valid recipient, the system will choose one allowed recipient for this turn.
- Only the selected recipient will see your message.
- You may pass useful information from one allowed recipient to the other allowed recipient.
- Submit ANSWER: <figure> only when messages provide evidence that all 5 agents have that figure.

Rules:
    Information:
    - Use only your private figures and messages received from allowed recipients.
    - Do not invent information or claim to have a figure that is not in your private list.
    - If you relay information, say which agent originally provided it.

    Sending messages:
    - For a CHAT message, choose one valid recipient from: {recipients_text}.
    - If another agent asked you a question, answer it first.
    - Send only one CHAT message or one ANSWER each turn.
    - Do not repeat your previous message.

    Sharing your list:
    - If you have not shared your full figure list yet, send it to one allowed recipient.
    - After that, discuss only useful matches, non-matches, and candidate figures.

    Discussion:
    - Compare received information with your private figures.
    - Share evidence about figures that may be common to all five agents.
    - If a discussed figure is not in your private list, clearly say that you do not have it.
    - Stop supporting any figure that is not in your private list.
    - Pass useful evidence to your other allowed recipient when needed.

    {answer_rule}
    Submitting:
    - Submit only in this format: ANSWER: <figure>
    - Submit only a figure that is in your private list.
    - Submit only when there is evidence that Agent1, Agent2, Agent3, Agent4, and Agent5 all have that figure.
    - Your private list counts as evidence for yourself.


Current state:

You are: {agent_name}
Your figures: {figures_list}
Allowed recipients: {recipients_text}
Suggested recipient for this turn if you need a default: {fallback_recipient}
Already shared your figures: {shared_full_list_text}
Previous message: {last_own_message}
Question to answer now: {pending_question_text}

Recent messages:
{history_text}

Output exactly one:

ACTION: CHAT
TO: <one of: {recipients_text}>
MESSAGE: <short useful message>
{answer_output}
